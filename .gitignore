# Local machine paths — copy from config.example.yaml
config.yaml

# Session notes and local agent config — do not publish
tasks/
.grok/

# Generated catalogs / Synty previews if someone points config in-repo
catalogs/
previews/
# Derived FTS index (rebuilt by scan / scan --index; JSON is source of record)
search.db

.venv/
venv/
.env
.env.*

__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.eggs/
dist/
build/
.wt/
